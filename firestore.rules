
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal access for authenticated users to ensure seamless tactical sync
    // in the field for both registered guardians and anonymous volunteers.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
