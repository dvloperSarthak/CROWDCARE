
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Globally authorized access for authenticated tactical users.
    // This handles all collections including users, children, rescueEvents, and broadcasts.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
